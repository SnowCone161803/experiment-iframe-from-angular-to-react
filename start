#! /usr/bin/env bash

set -o errexit
set -o nounset
set -o pipefail
#set -o xtrace

#[ `whoami` != root ] && echo "Must be run as root" && exit 1
#[ `whoami` == root ] && echo "Must NOT be run as root" && exit 1

cd angular
ng serve &

cd ..
cd other-angular
ng serve &

wait
